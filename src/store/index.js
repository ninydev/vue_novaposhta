import { createStore } from 'vuex'
import vuexDesign from '@/store/_modules/_design'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    vuexDesign
  }
})
